// const logger = require('../utils/logger');

const {ARG_TYPES, CATEGORIES, LEVELS} = require('../utils/constants');
const Command = require('../classes/Command');

// let ping = new Command(CATEGORIES.CORE, )